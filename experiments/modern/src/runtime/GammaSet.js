const MakiObject = require("./MakiObject");

class GammaSet extends MakiObject {
  /**
   * getclassname()
   *
   * Returns the class name for the object.
   * @ret The class name.
   */
  static getclassname() {
    return "GammaSet";
  }
}

module.exports = GammaSet;
