import MakiObject from "./MakiObject";

class Map extends MakiObject {
  /**
   * getclassname()
   *
   * Returns the class name for the object.
   * @ret The class name.
   */
  getclassname() {
    return "Map";
  }
}

export default Map;
