{
  "name": "webamp-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy": "yarn workspace webamp build && yarn workspace webamp-dot-org build"
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  },
  "prettier": {}
}
